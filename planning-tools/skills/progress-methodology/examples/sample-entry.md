<!-- This file is a fallback style exemplar for the /planning-tools:plan-progress composition routine.
     It is used only when a destination has no prior entries to learn from (cold start).
     The entry below is fabricated — names and SHAs are illustrative, not real. -->

<!-- planning-tools:plan-progress entry-key:demo-ticket-1234-add-thing -->
## DEMO-1234: Add the thing to the place — refactor of the foo module ✅

**Shipped 2026-05-12 via PR #999.** Closes [DEMO-1234](https://linear.app/example/issue/DEMO-1234). The foo module's `processBatch` function had been re-entered twice in the past sprint to graft on cache invalidation and retry semantics, and the resulting structure conflated three concerns: input validation, batch partitioning, and per-row dispatch. Last incident (2026-05-06, `aabbccd`) traced a 4-hour partial outage to a missed cache invalidation when the retry path re-used a stale partition map. **Architecture pivot:** split `processBatch` into three pure functions (`validateInput`, `partitionBatch`, `dispatchRows`) wired by a thin orchestrator in `src/foo/orchestrate.ts` — same public API, no consumer changes. Cache invalidation now lives in `dispatchRows` only (single owner; commit `1a2b3c4`), and the retry path receives a freshly-partitioned map instead of re-using the prior one (commit `1a2b3c4` lines 142-167). New `__tests__/foo/orchestrate.test.ts` adds 14 cases covering the stale-partition regression that triggered the 2026-05-06 incident; the synthetic seed reproduces the original failure mode in <100ms. **Piggybacked:** `RoaRequestsCard.tsx` truncation fix (commit `9z8y7x6`) — agent had flagged a sidebar overflow in a sibling ticket; default-render first 5 entries with a `Mehr anzeigen` toggle below; matches the FinanceCard pattern. **Verification:** typecheck ✅, lint ✅ (0 errors), `make test-shared` 247/247 ✅, `make test` 836/836 ✅, e2e foo-orchestrate spec 8/8 ✅, manual PROD smoke against synthetic batch ✅. **Out of scope:** Migration to the new `processBatch` interface for the legacy `bar` consumer (deferred to DEMO-1240 — separate refactor, scope already grew here). Cache TTL tuning based on the new dispatcher metrics — deferred until 1 week of post-deploy data is available.

---

<!-- planning-tools:plan-progress entry-key:demo-ticket-1230-fix-bar -->
## DEMO-1230: Fix bar.exceptionHandler swallow on async failure (in-flight)

**Branch:** `oliverborchers/demo-1230-bar-exception-handler` (3 commits, ready to PR). Closes [DEMO-1230](https://linear.app/example/issue/DEMO-1230). The `bar.exceptionHandler` middleware was swallowing async errors because the `Promise.catch` path ran on a closure that re-bound `this` after the handler was registered — every catch fired but the `report()` call resolved against a stale `this.logger`, producing `report is not a function` and aborting before the upstream `next(err)` could fire. Reproduced locally with the synthetic 2026-05-04 failure (commit `4d5e6f7`), confirmed root cause via `console.trace` injection in the catch path. **Architecture:** moved `report` and `next` to closure-captured locals at handler-registration time (commit `5e6f7a8`); added a single integration test that asserts `next` is called with the original `err` even when `report` throws (commit `6f7a8b9`). **Verification:** typecheck ✅, lint ✅, `make test-shared` 247/247 ✅, integration test `bar.exception-handler.test.ts` 4/4 ✅ — the regression suite from DEMO-1219 (the prior incident with this handler) still passes. Smoke against the 2026-05-04 reproduction case now produces the expected `next(err)` chain in <50ms. Still pending: deploy to staging and confirm Datadog `bar.exception_handler.errors` metric shows the expected pattern under synthetic load.
